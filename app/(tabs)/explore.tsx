import { StyleSheet, Image, Platform, Linking, TouchableHighlight, Text } from 'react-native';

import { Collapsible } from '@/components/Collapsible';
import { ExternalLink } from '@/components/ExternalLink';
import ParallaxScrollView from '@/components/ParallaxScrollView';
import { ThemedText } from '@/components/ThemedText';
import { ThemedView } from '@/components/ThemedView';
import { IconSymbol } from '@/components/ui/IconSymbol';

export default function TabTwoScreen() {
  return (
    <ParallaxScrollView
      headerBackgroundColor={{ light: '#D0D0D0', dark: '#353636' }}
      headerImage={
        <IconSymbol
          size={310}
          color="#808080"
          name="chevron.left.forwardslash.chevron.right"
          style={styles.headerImage}
        />
      }>
      <ThemedView style={styles.titleContainer}>
        <ThemedText type="title">Créditos</ThemedText>
      </ThemedView>
      <ThemedText>Mateus Vinicius Schons <TouchableHighlight onPress={() => Linking.openURL("https://github.com/MateusViniciusSchons")}><Text style={{color: 'white'}}>GitHub</Text></TouchableHighlight> fez esse app. Ele foi criado apenas para diversão.</ThemedText>
      <ThemedText>Aproveite para se ver no espelho ao comprar um produto.</ThemedText>
    </ParallaxScrollView>
  );
}

const styles = StyleSheet.create({
  headerImage: {
    color: '#808080',
    bottom: -90,
    left: -35,
    position: 'absolute',
  },
  titleContainer: {
    flexDirection: 'row',
    gap: 8,
  },
});
